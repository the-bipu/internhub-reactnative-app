import { useState } from "react";

import { View, ScrollView, SafeAreaView, Text, Image, Button, TouchableOpacity } from "react-native";

const Home = () => {
    return (
        <SafeAreaView className='flex bg-[#ffffff] w-full h-full items-center justify-center'>
            <Text className="bg-white text-black">
                hello
            </Text>
        </SafeAreaView>
    )
}

export default Home;